{
  "name": "valkompass-ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "fetch-politicians": "cd commands && bun install && bun run fetch-politicians",
    "extract-parties": "cd commands && bun install && bun run extract-parties",
    "test-fetch-votes": "cd commands && bun install && bun run test-fetch-votes",
    "performance-test": "cd commands && bun install && bun run performance-test",
    "concurrency-test": "cd commands && bun install && bun run concurrency-test",
    "fetch-votes": "cd commands && bun install && bun run fetch-votes",
    "test-fetch-voteringar": "cd commands && bun install && bun run test-fetch-voteringar",
    "fetch-voteringar": "cd commands && bun install && bun run fetch-voteringar",
    "enrich-voteringar": "cd commands && bun install && bun run enrich-voteringar",
    "test-enrich-voteringar": "cd commands && bun install && bun run test-enrich-voteringar"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.511.0",
    "neo4j-driver": "^5.28.1",
    "next": "15.3.2",
    "openai": "^4.103.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-markdown": "^10.1.0",
    "tailwind-merge": "^3.3.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.3.2",
    "tailwindcss": "^4",
    "tw-animate-css": "^1.3.0",
    "typescript": "^5"
  }
}
